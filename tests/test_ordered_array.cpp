#include <gtest.h>
#include "../lib_table_ordered_array/ordered_array.h"


TEST(TOrderedArray, can_create_ordered_array) {
  ADD_FAILURE();
}

TEST(TOrderedArray, can_insert_into_ordered_array) {
  ADD_FAILURE();
}

TEST(TOrderedArray, can_find_in_ordered_array) {
  ADD_FAILURE();
}

TEST(TOrderedArray, can_delete_from_ordered_array) {
  ADD_FAILURE();
}
