#include <gtest.h>
#include "../lib_table_tree/tree.h"

TEST(TTree, can_create_tree) {
  ADD_FAILURE();
}

TEST(TTree, can_insert_into_tree) {
  ADD_FAILURE();
}

TEST(TTree, can_find_in_tree) {
  ADD_FAILURE();
}

TEST(TTree, can_delete_from_tree) {
  ADD_FAILURE();
}
