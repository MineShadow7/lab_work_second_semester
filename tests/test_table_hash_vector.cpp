#include <gtest.h>
#include "../lib_table_hash_vector/hash_vector.h"

TEST(THashArray, can_create_hash_array) {
  ADD_FAILURE();
}

TEST(THashArray, can_insert_into_hash_array) {
  ADD_FAILURE();
}

TEST(THashArray, can_find_in_hash_array) {
  ADD_FAILURE();
}

TEST(THashArray, can_delete_from_hash_array) {
  ADD_FAILURE();
}
