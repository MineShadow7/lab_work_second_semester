#include <gtest.h>
#include "../lib_table_sorted_array/sorted_array.h"

TEST(TSortedArray, can_create_sorted_array) {
  ADD_FAILURE();
}

TEST(TSortedArray, can_insert_into_sorted_array) {
  ADD_FAILURE();
}

TEST(TSortedArray, can_find_in_sorted_array) {
  ADD_FAILURE();
}

TEST(TSortedArray, can_delete_from_sorted_array) {
  ADD_FAILURE();
}
