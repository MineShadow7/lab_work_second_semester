#include <gtest.h>
#include "../lib_table_linear_array/linear_array.h"

TEST(TLinearArray, can_create_linear_array) {
  ADD_FAILURE();
}

TEST(TLinearArray, can_insert_into_linear_array) {
  ADD_FAILURE();
}

TEST(TLinearArray, can_find_in_linear_array) {
  ADD_FAILURE();
}

TEST(TLinearArray, can_delete_from_linear_array) {
  ADD_FAILURE();
}
