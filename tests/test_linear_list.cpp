#include <gtest.h>
#include "../lib_table_linear_list/linear_list.h"

TEST(TLinearList, can_create_linear_list) {
  ADD_FAILURE();
}

TEST(TLinearList, can_insert_into_linear_list) {
  ADD_FAILURE();
}

TEST(TLinearList, can_find_in_linear_list) {
  ADD_FAILURE();
}

TEST(TLinearList, can_delete_from_linear_list) {
  ADD_FAILURE();
}
